#include <iostream>

using namespace std;

int main(){
    double sum, xor;
    double average;
    double a,b;
    int pairs;
    
    while (1){
          cin >>sum>>xor;
          average = sum / 2;
          if(xor == 1){a = average - 1;b = a + 2;}
          else {a = average;b = average;pairs = 1;}
          cout <<
          }
    
    
    
    system("pause");
    return EXIT_SUCCESS;
    }
